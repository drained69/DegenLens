# Runtime dependencies only. Kept minimal — every package here is imported by
# app/. `web3` and `tenacity` were previously declared but unused; dropping them
# removes ~50MB of transitive dependencies from the image.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<0.29
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-dotenv>=1.0,<2.0
